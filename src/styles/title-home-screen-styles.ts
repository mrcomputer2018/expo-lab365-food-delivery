import { StyleSheet } from "react-native";

export const titleHomeScreenStyles =  StyleSheet.create({
    containerRow: {
        flexDirection: "row",
        justifyContent: "flex-start",
        alignItems: "center",
        gap: 10,
        paddingHorizontal: 16,
    }
})