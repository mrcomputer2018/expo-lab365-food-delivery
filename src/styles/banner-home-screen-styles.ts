import { StyleSheet } from "react-native";

export const bannerHomeScreenStyles = StyleSheet.create({
    imageArea :{
        marginHorizontal: 12,
        marginTop: 16,
        justifyContent: "center",
        alignItems: "center"
    },
    image: {
        height: 200,
        width: "100%",
        resizeMode: "cover"
    }
})