import { StyleSheet } from "react-native";

export const footerFormStyles = StyleSheet.create({
    textButton: {
        color: "#66BE70",
        fontSize: 16,
        marginLeft: 10,
        fontWeight: "bold",
    },
});
